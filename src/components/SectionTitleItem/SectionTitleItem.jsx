const SectionTitleItem = ({ children }) => (
  <div
    style={{
      fontSize: "22px",
      margin: "10px 0",
      color: "#50ABFF",
    }}
  >
    {children}
  </div>
);

export default SectionTitleItem;
